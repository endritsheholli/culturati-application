pipeline {
    agent any

    environment {
        DEPLOYMENT_BRANCH          = "master"
    }

    stages {
        stage("Set Stages") {
            steps {
                sh 'printenv'
            }
        }
    }

}
